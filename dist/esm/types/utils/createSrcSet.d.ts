import { SrcSetArgs } from '../types';
declare const createSrcSet: ({ id, quality, sizesArr, format, switching, aspectRatio, apiVersion, cloudName, }: SrcSetArgs) => string;
export default createSrcSet;
